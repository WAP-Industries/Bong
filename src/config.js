const Settings = {
    PaddleSpeed: 10,
    BallSpeed: 10
}

const Game = {
    Sprites: [],
    GUI: [],
    Score: [0,0],
    LastWin: null,
    isPlaying: false
}

const Keys = {
    // left paddle keys
    W: false,
    S: false,

    // right paddle keys
    I: false,
    K: false
}